# 📖 使用指南

## 快速开始

1. 启动应用后，浏览器会自动打开（默认地址：`http://localhost:7860`）
2. 在"文件生成设置"区域：
   - 输入保存路径（例如：`/Users/username/Downloads`）
   - 输入文件名称（例如：`test_file.bin`）
   - 输入文件大小并选择单位（KB/MB/GB/TB）
3. 点击"开始生成文件"按钮
4. 在"磁盘空间监控"区域实时查看磁盘使用情况
5. 可以切换显示单位或手动刷新磁盘信息

## 使用示例

### 示例 1：生成 1GB 测试文件

- **保存路径**：`/Users/username/Downloads`
- **文件名称**：`test_1gb.bin`
- **文件大小**：`1`
- **单位**：`GB`

### 示例 2：生成 100MB 测试文件

- **保存路径**：`C:\Users\username\Desktop`
- **文件名称**：`test_100mb.bin`
- **文件大小**：`100`
- **单位**：`MB`

### 示例 3：生成 500KB 测试文件

- **保存路径**：`/tmp`
- **文件名称**：`small_test.bin`
- **文件大小**：`500`
- **单位**：`KB`

## 功能说明

### 文件生成

- **支持单位**：KB、MB、GB、TB
- **生成算法**：使用块写入算法（100MB 块），提高生成速度
- **自动验证**：自动验证文件大小和磁盘空间
- **错误处理**：完善的错误处理和提示
- **进度显示**：支持进度回调，实时显示生成进度

### 磁盘监控

- **实时显示**：实时显示所有磁盘分区的使用情况
- **智能过滤**：智能过滤系统分区（macOS/Windows）
- **单位切换**：支持多种显示单位切换（KB/MB/GB/TB）
- **详细信息**：显示使用率、已用空间、可用空间、总空间
- **刷新模式**：自动刷新和手动刷新两种模式

## 注意事项

1. **磁盘空间**：生成文件前请确保目标磁盘有足够空间
2. **文件路径**：确保目标路径存在且有写权限
3. **大文件生成**：生成 TB 级别文件可能需要较长时间，建议在 SSD 上生成
4. **系统资源**：生成大文件时会占用磁盘 I/O，可能影响系统性能

## 配置说明

### 修改默认端口

编辑 `main.py` 文件，修改 `app.launch()` 中的 `server_port` 参数：

```python
app.launch(
    server_name="0.0.0.0",  # 服务器地址
    server_port=8080,       # 修改端口号
    share=False             # 是否创建公共链接
)
```

### 启用公共链接

如果需要通过互联网访问，可以启用 `share=True`：

```python
app.launch(server_name="0.0.0.0", server_port=7860, share=True)
```

注意：公共链接会在 Gradio 服务器关闭后失效。

