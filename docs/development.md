# 💻 开发指南

## 项目结构

```
FileTools/
├── main.py                 # 主入口文件
├── pyproject.toml          # 项目配置和依赖管理
├── LICENSE                 # MIT 许可证
├── docs/                   # 文档目录
│   ├── CHANGELOG.md        # 版本变更记录
│   ├── English_README.md   # 英文 README
│   ├── contributing.md     # 贡献指南
│   ├── development.md      # 开发指南
│   ├── installation.md     # 安装指南
│   ├── usage.md            # 使用指南
│   ├── testing.md          # 测试文档
│   ├── logging.md          # 日志文档
│   └── faq.md              # 常见问题
├── src/                    # 源代码目录
│   └── filetools/          # 主包
│       ├── __init__.py
│       ├── config/         # 配置模块
│       │   ├── __init__.py
│       │   ├── constants.py # 常量定义（单位映射、块大小等）
│       │   └── logger.py   # 日志配置模块
│       ├── models/          # 业务逻辑模块
│       │   ├── __init__.py
│       │   ├── file_generator.py  # 文件生成逻辑
│       │   ├── disk_monitor.py    # 磁盘监控逻辑
│       │   └── disk_usage.py      # 磁盘使用情况数据模型
│       └── ui/              # UI 模块
│           ├── __init__.py
│           └── interface.py # Gradio 界面组件
├── tests/                  # 测试模块
│   ├── __init__.py
│   ├── test_constants.py  # 常量测试
│   ├── test_disk_monitor.py      # 磁盘监控测试
│   ├── test_file_generator.py    # 文件生成测试
│   ├── test_file_generator_core.py  # 文件生成核心功能测试
│   ├── test_ui_functions.py     # UI 函数测试
│   └── test_ui_validation.py     # UI 验证测试
├── docs/                   # 文档目录
├── assets/                 # 资源文件目录
└── logs/                   # 日志目录（自动创建，已加入 .gitignore）
    └── filetools.log       # 应用日志文件
```

## 技术架构

- **前端框架**：Gradio（Python Web UI 框架）
- **系统监控**：psutil（跨平台系统监控库）
- **文件操作**：Python 标准库 `pathlib`, `io`
- **测试框架**：pytest
- **架构模式**：模块化设计，清晰的职责分离

## 代码规范

### 编码规范

- 严格遵循 **PEP 8** 编码规范
- 公共函数/类/模块必须有 docstring
- 使用类型注解（typing）提升代码可维护性
- 函数单一职责，行数 ≤ 50
- 完善的异常处理和日志记录

### 代码风格

- 使用 4 个空格缩进
- 行长度不超过 88 字符（Black 标准）
- 使用有意义的变量和函数名
- 避免使用魔法数字，使用常量定义

### 文档字符串格式

```python
def function_name(param1: str, param2: int) -> bool:
    """
    函数功能描述
    
    :param param1: 参数1的说明
    :param param2: 参数2的说明
    :return: 返回值的说明
    """
    pass
```

## 运行配置

### 默认配置

- 服务器地址：`0.0.0.0`
- 端口：`7860`
- 共享链接：关闭

### 修改配置

可在 `main.py` 中修改启动参数：

```python
app.launch(
    server_name="0.0.0.0",  # 服务器地址
    server_port=7860,       # 端口号
    share=False             # 是否创建公共链接
)
```

## 开发环境设置

```bash
# 安装开发依赖（包括 pytest）
uv sync --extra dev

# 或使用传统方式
pip install -e ".[dev]"
```

## 模块说明

### filetools.config 模块

- `constants.py`: 定义项目常量，如单位映射、块大小等
- `logger.py`: 配置日志系统

### filetools.models 模块

- `file_generator.py`: 文件生成核心逻辑
- `disk_monitor.py`: 磁盘监控逻辑
- `disk_usage.py`: 磁盘使用情况数据模型

### filetools.ui 模块

- `interface.py`: Gradio 界面组件和交互逻辑

## 添加新功能

1. 在相应的模块中添加功能代码
2. 编写单元测试（见 [测试文档](testing.md)）
3. 更新相关文档
4. 确保代码通过 lint 检查

## 调试技巧

### 查看日志

```bash
# 实时查看日志
tail -f logs/filetools.log
```

### 启用调试模式

修改 `src/filetools/config/logger.py` 中的日志级别：

```python
logger = setup_logger(level=logging.DEBUG)
```

### 使用调试器

```bash
# 使用 pdb
python -m pdb main.py

# 或使用 IDE 调试功能
```

## 打包

如果需要打包为独立应用，可以使用 PyInstaller：

```bash
# 安装 PyInstaller
uv add pyinstaller

# 打包为单文件（使用 upx 压缩，需要先安装 upx）
pyinstaller --onefile --noconsole --upx-dir=path/to/upx main.py

# 打包为单文件（不使用 upx）
pyinstaller --onefile --noconsole main.py
```

