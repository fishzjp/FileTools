[project]
name = "filetools-main"
version = "0.1.0"
description = "A file size generation tool with disk monitoring, built with Gradio"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "psutil>=7.1.3",
    "gradio>=4.0.0",
]
